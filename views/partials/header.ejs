<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1"><!-- Make site responsive on mobile/tablet -->
	<title>Yelp Camp</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
	<link rel="stylesheet" href="/stylesheets/main.css">
</head>
<body>
	<nav class="navbar navbar-default">
		<div class="container">
			<div class="navbar-header">
				<a class="navbar-brand" href="/">Camp</a>
				<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
					<span class="sr-only">Toggle navigation</span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
				</button>
			</div>

			<div id='navbar' class="collapse navbar-collapse">
				 <ul class="nav navbar-nav">
					<li class="<%= typeof page !== 'undefined' && page === 'campgrounds' ? 'active' : '' %>"><a href="/campgrounds">Home</a></li>
				</ul>
				<ul class="nav navbar-nav navbar-right">
				<% if(currentUser){ %>
					<li><a href="/profile/<%= currentUser.id %>">Signed in as <%= currentUser.username %></a></li>
					<li><a href="/logout">Logout</a></li>
				<% } else { %>
					
					<% if(typeof page !== 'undefined' && page === 'login'){ %>
						<li class='active'><a href="#">Login</a></li>
				  	<% }else{ %>
						<li><a href="/login">Login</a></li>
				  	<% } %>
					
					<% if(typeof page !== 'undefined' && page === 'register'){ %>
						<li class='active'><a href="#">Sign Up</a></li>
				  	<% }else{ %>
						<li><a href="/register">Sign Up</a></li>
				  	<% } %>
		
				<% } %>
				</ul>
			</div>
		</div>
	</nav>

	<div class='container'>
		<% if(success && success.length){ %>
			<div class="alert alert-success" role="alert"><%= success %></div>		
		<% } %>
		<% if(error && error.length){ %>
			<div class="alert alert-danger" role='alert'><%= error %></div>
		<% } %>
	</div>
